{
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna clean --yes",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "publish:canary": "lerna publish from-package --canary --yes",
    "publish:production": "lerna publish from-package --yes"
  },
  "name": "mitei-server",
  "private": true,
  "license": "GPL-3.0",
  "devDependencies": {
    "@material-ui/core": "^4.11.0",
    "@material-ui/icons": "^4.9.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
